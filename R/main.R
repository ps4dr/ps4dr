#' The main function
#'
#' This function runs the whole MSDRP pipeline
#' @param love Do you love cats? Defaults to TRUE.
#' @keywords pipeline
#' @export 
main <- function(debug=False) {
    if(debug == TRUE){
        print("Debugging is on")
    }
    else {
        print("Debugging is off")
    }
}
